<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="x.pandoraapp.views.HomeFragment"
        android:label="Home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/nav_services"
        android:name="x.pandoraapp.views.ServicesFragment"
        android:label="Serviços"
        tools:layout="@layout/fragment_services" />

    <fragment
        android:id="@+id/nav_chat"
        android:name="x.pandoraapp.views.ChatFragment"
        android:label="Chat"
        tools:layout="@layout/fragment_chat" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="x.pandoraapp.views.SettingsFragment"
        android:label="Perfil"
        tools:layout="@layout/fragment_settings" />

</navigation>